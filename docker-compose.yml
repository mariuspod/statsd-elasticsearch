version: '3'
services:
  statsd-backend:
    build: .
    depends_on:
      - elasticsearch
    networks:
      - statsd_net
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:5.2.2
    ports:
      - "9200:9200"
    networks:
      - statsd_net

networks:
  statsd_net:
